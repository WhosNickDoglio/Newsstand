name: Setup
description: 'Setup stuff'
runs:
  using: "composite"
  steps:
    - name: set up JDK 19
      uses: actions/setup-java@v3.10.0
      with:
        distribution: 'zulu'
        java-version: '19'
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2.3.3
